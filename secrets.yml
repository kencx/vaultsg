foo:
  baz:
    data:
      # generate a password of length 24 with special chars
      - key: password
        length: 24
        special: true
  bar:
    bat:
      data:
        # set the following static secrets
        - key: username
          secret: foo
        - key: password
          secret: password
